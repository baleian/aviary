{{- if .Values.extraEgress }}
{{/*
   Per-environment NetworkPolicy. Unions on top of the aviary-platform
   baseline-egress NetworkPolicy (K8s evaluates egress as a disjunction).
*/}}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "aviary-runtime.fullname" . }}-egress
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "aviary-runtime.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      aviary/environment: {{ include "aviary-runtime.name" . }}
  policyTypes: ["Egress"]
  egress:
    {{- toYaml .Values.extraEgress | nindent 4 }}
{{- end }}
