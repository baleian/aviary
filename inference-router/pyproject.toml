[project]
name = "agentbox-inference-router"
version = "0.1.0"
description = "AgentBox Inference Router — single endpoint for all LLM backends"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pydantic",
]
