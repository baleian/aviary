[project]
name = "aviary-secret-provider"
version = "0.1.0"
description = "Aviary Secret Provider — resolves credential references to secrets from Vault"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "asyncpg",
    "pydantic",
]
