[project]
name = "aviary-egress-proxy"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "asyncpg",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
