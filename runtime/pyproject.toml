[project]
name = "agentbox-runtime"
version = "0.1.0"
description = "AgentBox Agent Runtime — runs inside session Pods"
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk",
    "anthropic",
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pydantic",
]
