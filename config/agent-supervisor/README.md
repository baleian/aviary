# This directory is populated by scripts/setup-dev.sh with the K3s ServiceAccount
# token and CA certificate that agent-supervisor uses to authenticate to the
# K3s API (k8s-proxy endpoint mode). The generated files are gitignored.
